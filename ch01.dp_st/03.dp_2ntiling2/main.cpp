#include <cstdio>
int d[1001];
int main() {
    d[0]=1;
    d[1]=1;
    int n;
    while(1) {
        printf("Input n(n<=0 to quit) => ");
        scanf("%d",&n);
        if(n<=0) break;

        .........

        printf("%d\n\n",d[n]);
    }

    return 0;
}
